/**
 * Program Name:    cis27spring2019Program01.c
 * Discussion:      Dynamic Memory
 */
 
 #include<stdio.h>

// Function Prototypes
int getTotalUniqueDigitCount(int ary[]);
int getLargestUniqueDigitCount();
void doMenu(void);

// Appication Driver
int main() {
    // Declare all variables at top of function

    // Clearing pointer values
    int* iPtr = 0;
    int* iPtr = NULL;


    return 0;
}

// Use pointers to send arrays into functions
int getTotalUniqueDigitCount(int &ary[]) {
    // loop through array to get count
    // get largest total unique digit count
    // Largest TUDC of each element in array
    // int largestTUDC
    // int largestValue

    // if lgstTUDC = getTUDC(ary[0])
    // lgstTUDC = ary[0]


    // make dgtCntAry a pointer
    int dgtCountAry[10] = {0};
    int tudc = 0;

    if tudc (ary < 0)

    do {
        dgtCountAry[tudc%10]++
        tudc /= 10;

    } while (tudc);
    
}

int getLargestUniqueDigitCount() {
    // if (largestTUDC == getTUDC(ary[i]))
    // if (largestValue < ary[i]) { largestValue = ary[i] } 

}

int getLargestValue(int ary[]) {
    int largest;
    largest = 0;
    // loop through array and compare against largest, then set largest value to largest variable

    return largest;
}

